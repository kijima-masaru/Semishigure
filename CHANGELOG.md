# Changelog

## 1.1.1 (2026-09-06)

- 画面と README から、開発時の検証用 PBX（`deploy/`）への案内を外した。対象は稼働中の PBX であり、利用者が PBX を立てる必要はない

## 1.1.0 (2026-09-06)

- 「デバッグ」タブを追加。アプリ自身のログ（最新 1000 行）と環境情報（版、OS、Python、データの場所、ログファイル）を表示し、「すべてコピー」で不具合報告用の文面として取り込める。画面側の JavaScript エラーも記録
- ヘッダーに更新アイコンを追加。起動時とアイコン押下時に GitHub の最新リリースを確認し、新しい版があれば案内する。Windows デスクトップ版はその場でインストーラをダウンロードして検証（SHA-256）し、アプリを閉じて更新、終わると自動で再起動する。それ以外の起動方法ではリリースページへ案内
- タブ名: セットアップガイド / PBX / シナリオ / 実行 / 結果 / デバッグ
- 狭い画面でタブが横にはみ出さないよう折り返すようにした

## 1.0.5 (2026-09-06)

- ヘッダーのタブを設定する順に並べ替えた: ガイド → PBX → シナリオ → 実行 → 結果

## 1.0.4 (2026-09-06)

- ガイド 1 の右側の説明を、項目ごとに開閉できるアコーディオンにした（閉じた状態では見出しだけ。開くと短い文で行を分けた説明が出る）
- シナリオの「新規作成」を押すと必ず作成フォームが開き、名前欄にフォーカスが移るようにした（シナリオが 1 つも無いときはフォームが最初から開いていて、押しても変化が無いように見えていた）。フォームに見出しと説明を付けた
- 画面と静的ファイルに Cache-Control: no-cache と版付きの URL を付け、アプリを更新した直後に前の版の画面が残らないようにした

## 1.0.3 (2026-09-06)

画面の改修。

- ガイド 1（PBX に接続）の説明を書き直した。このアプリが PBX に対して何をするか、PBX 側で通しておく 3 点（SIP / RTP、管理用ポート、ログ）、接続テストで確かめる内容を平易に説明
- シナリオ タブに「新規作成」を追加。名前を入れて、雛形から作るか選択中のシナリオをコピーするかを選べる。ガイドへの導線も付けた
- ヘッダーからアプリ名、最終更新時刻、「ランなし」を外し、代わりに PBX との接続状態を 1 行で表示（未接続 / 接続テストの結果 / ラン中は「接続しています…」「接続済み」「接続できません」と SIP 登録数・監視の状態 / アプリのサーバとの切断）。色は 緑 = 接続済み・合格、黄 = 接続の途中、赤 = 接続できない・NG・サーバと切断

## 1.0.2 (2026-09-06)

- Windows のアプリ窓を Edge / Chrome のアプリ モード（別プロセス）に変更。WinForms + WebView2（pywebview）は、環境によって .NET の例外でプロセスごと落ちることがあったため廃止
- インストーラから pywebview / pythonnet などを除いた

## 1.0.1 (2026-09-06)

Windows デスクトップ版の修正。

- スタートメニューのショートカットを「Semishigure」の 1 つだけにした（名前が文字化けしていたコンソール版は削除。コンソール起動はコマンド `semishigure-desktop` に）
- 起動に失敗したときに黙って終了せず、理由をダイアログで表示し `desktop.log` に記録するようにした。アプリ窓（WebView2）が開けない場合は既定ブラウザで開く
- リリースワークフローでインストーラの無人インストール → 起動 → 画面到達を検査するようにした
- リリースワークフローの手動実行（`tag` 入力）でタグと Release を作れるようにした

## 1.0.0 (2026-09-06)

最初のリリース。

- SIP / RTP スタック内蔵の同時通話負荷検証（発信側と応答側、UDP / TCP / TLS、G.711、DTMF、REFER）
- 目標同時数のリアルタイム制御（段階実行、プリセット、バースト、自動減少、prod の確認と上限）
- PBX 接続と監視（FreeSWITCH の ESL、Asterisk の AMI、SSH 経由、プロセスとログ、イベントと切断理由）
- プラグイン機構（log_patterns / status_command / conf_override / ws_hook / webhook）
- 記録（SQLite、記録表 xlsx、画面での詳細と比較）
- 画面（ガイド / 実行 / シナリオ / PBX / 結果、ライト / ダーク、アクセシビリティ検査）
- セットアップガイド（PBX 接続 → 内線を作る → 内線と番号 → シナリオ → 事前チェック → 負荷検証）
- PBX 側の内線と着信グループの作成と削除（FreeSWITCH / Asterisk / FusionPBX）
- Windows デスクトップ版（インストーラ、アプリ窓）
